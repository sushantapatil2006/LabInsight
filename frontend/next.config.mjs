/** @type {import('next').NextConfig} */
const nextConfig = {
  allowedDevOrigins: [
    "192.168.1.5",
    "landless-poster-skyrocket.ngrok-free.dev",
    "localhost",
    "127.0.0.1",
  ],
};

export default nextConfig;
